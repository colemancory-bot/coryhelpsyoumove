# Large media files (host on CDN separately)
*.mp4
*.mov
*.avi
*.mkv

# Environment / secrets
.env
.env.*

# Supabase local config (secrets handled via Supabase CLI env vars)
supabase/.env
supabase/.temp/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / tools
.vscode/
.idea/
.claude/
*.swp
*.swo

# Build scripts (not needed in production)
_generate-pages.js

# Node (if added later)
node_modules/
